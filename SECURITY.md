# 安全与数据

本版本是单用户本机应用，只监听回环地址，不包含公网认证、租户隔离或云端部署组件。请勿直接通过端口转发或反向代理公开运行。

API密钥以本机文件保存，不是加密存储。不要公开 `data/`、备份、个人导出文件或带私人内容的截图。远程模型会收到完成用户请求所需的材料；请使用可信的服务。

发现问题时，请优先通过仓库可用的私人漏洞报告入口联系维护者；若该入口不可用，可先提交不含攻击细节和私人信息的 Issue 请求联系渠道。不要在公开 Issue 附上可用密钥、数据库或针对第三方的利用过程。
